apiVersion: v1
kind: Service
metadata:
  name: fullstack-api
  labels:
    component: fullstack-api
    tier: backend
spec:
  selector:
    component: fullstack-api
    tier: backend
  ports:
    - port: 3000
      targetPort: 3000
